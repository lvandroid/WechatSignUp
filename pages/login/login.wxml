<!-- pages/login/login.wxml -->
<view class="container">
    <view class="userinfo">
        <button class="btn" type="warn" loading="{{loading}}" disabled="{{disabled}}" open-type="getUserInfo" bindgetuserinfo="doLogin">
            微信登录
        </button>
    </view>
</view>